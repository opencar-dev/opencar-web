---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span class:list={["absolute w-4 h-4 border-primary/25 top-0 left-0 border-t border-l", className]} />
<span class:list={["absolute w-4 h-4 border-primary/25 top-0 right-0 border-t border-r", className]} />
<span class:list={["absolute w-4 h-4 border-primary/25 bottom-0 left-0 border-b border-l", className]} />
<span class:list={["absolute w-4 h-4 border-primary/25 bottom-0 right-0 border-b border-r", className]} />
