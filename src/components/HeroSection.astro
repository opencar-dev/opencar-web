---
import { ArrowRight } from "@lucide/astro";
import CrosshairNode from "./CrosshairNode.astro";
import DimensionLabel from "./DimensionLabel.astro";
---

<section class="relative min-h-[70vh] flex items-center justify-center overflow-hidden">
  <div class="absolute inset-0 scanline pointer-events-none"></div>

  <div class="absolute top-1/4 left-1/4 w-96 h-96 rounded-full bg-primary/5 blur-[120px]"></div>
  <div class="absolute bottom-1/4 right-1/4 w-64 h-64 rounded-full bg-accent/5 blur-[100px]"></div>

  <CrosshairNode class="top-8 left-8 !w-4 !h-4 hidden md:block" />
  <CrosshairNode class="top-8 right-8 !w-4 !h-4 hidden md:block" />
  <CrosshairNode class="bottom-8 left-8 !w-4 !h-4 hidden md:block" />
  <CrosshairNode class="bottom-8 right-8 !w-4 !h-4 hidden md:block" />

  <div class="absolute top-8 left-1/2 -translate-x-1/2 hidden md:block">
    <DimensionLabel>opencar.dev</DimensionLabel>
  </div>

  <div class="relative z-10 container mx-auto px-6 text-center">
    <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-primary/20 bg-primary/5 mb-8 animate-slide-up">
      <span class="w-2 h-2 rounded-full bg-primary animate-pulse-glow"></span>
      <span class="text-sm font-mono text-primary">The Open Standard for the Global Automotive Ecosystem</span>
    </div>

    <h1 class="mb-6 animate-slide-up" style="animation-delay: 0.1s;">
      <img src="/logo.png" alt="OpenCar" class="h-16 md:h-24 lg:h-32 mx-auto" />
    </h1>

    <p class="max-w-2xl mx-auto text-lg md:text-xl text-muted-foreground font-sans leading-relaxed mb-10 animate-slide-up" style="animation-delay: 0.2s;">
      The common language that allows websites, apps, and hardware to talk to one another.
      Ensuring automotive culture—in all its forms—thrives in a connected world.
    </p>

    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 animate-slide-up" style="animation-delay: 0.3s;">
      <a
        href="#standards"
        class="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-primary text-primary-foreground font-mono font-semibold text-sm hover:shadow-[var(--glow-primary)] transition-all duration-300"
      >
        Explore Standards
        <ArrowRight class="w-4 h-4" />
      </a>
      <a
        href="https://github.com/opencar-dev"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-border text-foreground font-mono font-semibold text-sm hover:border-primary/50 hover:bg-primary/5 transition-all duration-300"
      >
        View on GitHub
      </a>
    </div>
  </div>
</section>
