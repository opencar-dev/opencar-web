---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span class:list={["inline-flex items-center gap-1 text-[10px] font-mono text-primary/30 uppercase tracking-widest", className]}>
  <span class="w-3 h-px bg-primary/20"></span>
  <slot />
  <span class="w-3 h-px bg-primary/20"></span>
</span>
